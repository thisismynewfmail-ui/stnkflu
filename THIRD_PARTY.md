# Sources and attribution

- The connectome importer, inferred visual projection, spiking kernel and baseline-centered plasticity implementation are adapted from [DOOMFLY](https://github.com/nftechie/doomfly), copyright © 2026 nftechie and DOOMFLY contributors, under the MIT License preserved in `LICENSE`. FLYLAB adds the channel atlas, the general-purpose runtime, the device layer, the workflow language and engine, the interface, and the configurable compartment selection. No Doom game assets, World Labs assets or movie clips are included.
- [MaleCNS v1.0](https://male-cns.janelia.org/) data: the MaleCNS collaboration and upstream contributors, distributed under the release's Creative Commons Attribution 4.0 terms. Downloaded separately; see `flylab/neural/datasets.json` and `sources.lock.json` for release URLs and checksums. Cite the dataset and its associated paper when publishing results. The retained graph and modelled physiology here are derived interpretations, not an official dataset product.
- [FastAPI](https://github.com/fastapi/fastapi) and [Starlette](https://github.com/encode/starlette) (MIT), [Uvicorn](https://github.com/encode/uvicorn) (BSD-3-Clause), [Pydantic](https://github.com/pydantic/pydantic) (MIT), [NumPy](https://numpy.org/) (BSD-3-Clause), [pandas](https://pandas.pydata.org/) (BSD-3-Clause), [Apache Arrow](https://arrow.apache.org/) (Apache-2.0) and [Pillow](https://python-pillow.org/) (MIT-CMU) are installed dependencies. Their licences remain with their packages.
- Optional device backends — mss, OpenCV, pyserial, gpiozero, lgpio, RPi.GPIO, pynput, sounddevice, paho-mqtt, Playwright — are imported only when a block needs them, and are not vendored here.
- The interface uses no web frameworks, no bundler and no external fonts or scripts. The files the browser runs are the files in `flylab/web/`.
- Scientific sources informing the model are linked in `docs/model.md` and beside the channels they support in `flylab/neural/atlas.py`. No papers or figures are redistributed.
- `assets/flylab.png` is newly generated project artwork; its generation prompt is in `assets/image-generation.md`.
